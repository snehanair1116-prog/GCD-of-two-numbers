# GCD-of-two-numbers
Greatest Common Divisor (GCD) This implementation uses the Euclidean Algorithm, which is the most efficient way to find the largest positive integer that divides two numbers without a remainder.
